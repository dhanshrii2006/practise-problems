1. A number is called balanced if the sum of digits at even positions equals the
sum of digits at odd positions.
Write a program to determine whether a given number is balanced.

2. Given three sides of a triangle, first validate whether a triangle is possible.
If valid, classify it as equilateral, isosceles, or scalene.

3. A shop offers a discount based on total purchase amount:
o < ₹1000 → No discount
o ₹1000–₹4999 → 5%
o ₹5000–₹9999 → 10%
o ≥ ₹10000 → 15%
Compute the final bill and discount applied.




4. Given a character input, determine whether it is:
o Uppercase letter
o Lowercase letter
o Digit
o Special symbol



5. Given a number, determine whether it is a spy number (sum of digits equals
product of digits).


6. A number is called wave number if its digits alternately increase and decrease.Check whether a given number is a wave number.


24 left 

7. Print all numbers between 1 and N that are divisible by the sum of their digits.

8. Given a number, rotate its digits to the right by K positions and print the new
number.

9. Generate all palindromic numbers between two given limits.

10.Given a number N, find the smallest number greater than N that is a
palindrome.

11.A number is called a twisted prime if the number and its reverse are both
prime.Check whether a given number satisfies this property.

12.Determine whether a number is a circular prime.

13.Given a number, check whether it can be expressed as the sum of
consecutive natural numbers.

14.Find the number of trailing zeros in the factorial of a given number without
actually computing the factorial.

-----------------------------------------------------------------------------------

15.Given two numbers A and B, determine whether they are friendly pairs
(ratio of sum of divisors to the number is same). <>

16.Given an array, find the length of the longest increasing contiguous
subarray.
17.Rearrange an array such that all negative numbers appear before positive
numbers without changing their relative order.
18.Given an array, determine whether it can be divided into two parts with
equal sum.
19.Find all leaders in an array
(an element is a leader if it is greater than all elements to its right).
20.Given an array, rotate it left by K positions using minimum extra space.

21.Given a string, check whether it is a pangram.
22.Find the first non-repeating character in a string.
23.Determine whether two strings are anagrams without sorting them.
24.Given a sentence, reverse each word individually while maintaining word
order.
25.Compress a string using run-length encoding
(e.g., aaabbc → a3b2c1).
26.Simulate a traffic signal system where signals change every T seconds.
Given current signal and remaining time, predict the next signal.
27.Simulate a simple ATM withdrawal system considering balance, minimum
balance rule, and denomination availability.
28.Given daily temperatures for N days, find how many days one has to wait
for a warmer temperature (logic only, no libraries).
29.Simulate a library book issue system: Maximum 3 books per student. Fine
of ₹5 per day after due date. Calculate fine and validity of issue.
30.Given a matrix, check whether it is a magic square.
